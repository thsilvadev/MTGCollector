/node_modules
.env

# Tesseract language data — auto-downloaded at runtime (~7 MB each)
*.traineddata

# Generated by: npm run build-icons  (re-run after deploy)
/data/icons/
/data/setIconIndex.json

# Debug images — only created locally with DEBUG_OCR=1
/data/debug/
